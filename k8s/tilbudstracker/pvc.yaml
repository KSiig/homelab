apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tilbudstracker-data
  namespace: tilbudstracker
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 1Gi
